% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/camera-pre-processing.R
\name{wt_image_datetime}
\alias{wt_image_datetime}
\title{Extract image datetimes from EXIF metadata}
\usage{
wt_image_datetime(
  paths,
  method = c("fast", "exact"),
  group = NULL,
  n = 5,
  tz = NULL
)
}
\arguments{
\item{paths}{A character vector of image file paths, typically produced by
\code{\link[=wt_image_paths]{wt_image_paths()}}.}

\item{method}{Character string specifying the extraction method. \code{"exact"}
extracts EXIF metadata from every image. \code{"fast"} extracts metadata from
the first and last \code{n} images, optionally within groups.}

\item{group}{An optional vector used to group images (e.g., site), typically
produced by \code{\link[=wt_extract_folder_level]{wt_extract_folder_level()}}. Must have the same length as
\code{paths}. When provided, the \code{"fast"} method extracts metadata from the
first and last \code{n} images within each group.}

\item{n}{Number of images to sample from the beginning and end of each
group when \code{method = "fast"}. Defaults to 5.}

\item{tz}{Time zone used when converting \code{DateTimeOriginal} to a
\code{POSIXct} object. Must be one of the values returned by
\code{\link[=OlsonNames]{OlsonNames()}}. If \code{NULL} (default), datetimes are returned as
character strings.}
}
\value{
A data frame with one row per input image containing:
\itemize{
\item \code{path}: image file path
\item \code{group}: grouping variable, if supplied
\item \code{DateTimeOriginal}: the extracted datetime. This is returned
as a \code{POSIXct} object when \code{tz} is supplied and as a character vector
otherwise.
}
}
\description{
Uses \pkg{exifr} to extract the \code{DateTimeOriginal} field from image EXIF
metadata. Images can either be processed exactly or sampled to provide a
faster estimate of the date range represented within a dataset.
}
\details{
The \code{"fast"} method is intended for quickly assessing the datetimes
of a large image dataset without reading EXIF metadata from every file.
Images that are not sampled will have \code{NA} values for their extracted
datetime. This is a fast way to quickly check if there are any datetime
errors in your image set.

The \code{"fast"} method assumes that \code{paths} are ordered chronologically within
groups. Users should ensure that file paths are sorted appropriately before
running this function.
}
\examples{
\dontrun{

imgs <- wt_image_paths("my_sample")

# Extract metadata from all images
wt_image_datetime(
  imgs,
  method = "exact"
)

# Quickly estimate date range by site
wt_image_datetime(
  imgs,
  method = "fast",
  group = site,
  tz = "US/Central"
)

}
}
