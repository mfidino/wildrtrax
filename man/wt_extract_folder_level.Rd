% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/camera-pre-processing.R
\name{wt_extract_folder_level}
\alias{wt_extract_folder_level}
\title{Extract a folder level from file paths}
\usage{
wt_extract_folder_level(paths, level, path_split = NULL)
}
\arguments{
\item{paths}{A character vector of file paths, typically produced by
\code{\link[=wt_image_paths]{wt_image_paths()}}.}

\item{level}{A positive integer specifying the folder level to extract.
Folder levels can be identified using \code{\link[=wt_path_summary]{wt_path_summary()}}.}

\item{path_split}{Character string used to split file paths into their
component folders. If \code{NULL}, defaults to the operating system's file
separator.}
}
\value{
A character vector containing the folder name at the requested level for
each input file path.
}
\description{
Extracts the folder name at a specified level of a file path hierarchy.
This is useful for creating grouping variables (e.g., site identifiers)
from image directory structures.
}
\details{
File paths are first stripped of their filenames before being split into
folder components. The requested folder level is then extracted from each
path.

This function is primarily intended to create grouping variables for
downstream functions such as \code{\link[=wt_image_datetime]{wt_image_datetime()}}.
}
\examples{
\dontrun{

imgs <- wt_image_paths("my_sample")

# Extract the first folder level (e.g., site)
site <- wt_extract_folder_level(
  imgs,
  level = 1
)

# Extract the second folder level
camera <- wt_extract_folder_level(
  imgs,
  level = 2
)

}

}
